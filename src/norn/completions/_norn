#compdef norn

_norn() {
    case "$words[2]" in
        add)
            compadd block interval
            ;;
        delete)
            compadd block interval
            ;;
        list)
            compadd block interval date
            ;;
        *)
            compadd start stop status app add delete list
            ;;
    esac
}

_norn