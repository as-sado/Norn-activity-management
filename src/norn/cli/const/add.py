
add_help = """add a time-based block (add block) or add an interval-based block (add interval)

block - how many minutes the app can be active during the day
    norn add block [initialclass app] [number of minutes]

interval - specifies the time interval during which the app will be locked
    norn add interval [InitialClass app] [start-time] [end-time]
        format for [start-time] and [end-time] HH:MM

"""


add_block_help = """
block - how many minutes the app can be active during the day
    norn add block [initialclass app] [number of minutes]"""


add_interval_help = """
interval - specifies the time interval during which the app will be locked
    norn add interval [InitialClass app] [start-time] [end-time]
        format for [start-time] and [end-time] HH:MM"""

